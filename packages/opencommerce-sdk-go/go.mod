// Module path is a local placeholder — see README.md's own "Module path"
// note before publishing this SDK anywhere real (e.g. a GitHub repository
// under whatever org ends up hosting it). It resolves correctly for local
// development, `go build`, and `go test` exactly as written.
module opencommerce-sdk-go

go 1.21
