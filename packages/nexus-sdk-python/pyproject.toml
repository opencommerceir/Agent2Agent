[project]
name = "nexus-sdk"
version = "0.1.0"
description = "Official Python client for the Nexus Public REST API."
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
