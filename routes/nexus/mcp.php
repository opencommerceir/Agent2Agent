<?php

/*
|--------------------------------------------------------------------------
| Nexus MCP Routes
|--------------------------------------------------------------------------
|
| Loaded by NexusServiceProvider::boot() via loadRoutesFrom(). Phase 0:
| no MCP tools yet — this reuses the existing MCP Gateway (routes/mcp.php,
| loaded by CoreServiceProvider) rather than standing up a second one.
| Nexus-specific capabilities/tools register their own route groups here
| once Negotiation/Contract/etc. exist.
|
*/
