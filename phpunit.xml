<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <coverage>
        <report>
            <html outputDirectory="coverage/html"/>
            <text outputFile="coverage/coverage.txt"/>
            <clover outputFile="coverage/clover.xml"/>
        </report>
    </coverage>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <!-- Phase 6, Stage 3 (§7.28) — explicit, not just relying on
             config/agent-orchestrator.php's own default: the whole suite
             must never attempt a real LLM network call. Tests that
             specifically exercise LLMPlanner override this per-test via
             config(['agent-orchestrator.planner.type' => 'llm']) after
             also rebinding LLMClientInterface to a fake. -->
        <env name="PLANNER_TYPE" value="deterministic"/>
        <!-- Phase 6, Stage 6 (§7.31) — same reasoning as PLANNER_TYPE above:
             explicit, not just relying on config/agent-orchestrator.php's
             own default, so the whole suite never attempts a real LLM
             network call for reasoning either. Tests that specifically
             exercise LLMReasoningEngine override this per-test via
             config(['agent-orchestrator.reasoning.type' => 'llm']) after
             also rebinding LLMClientInterface to a fake. -->
        <env name="REASONING_TYPE" value="simple"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
